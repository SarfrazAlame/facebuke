import React from "react";

const MoreLikes = async ({ userId }: { userId: string }) => {
//   const likes = await fetchLikes();
//   console.log(likes.filter((like)=> like.userId===userId));

  return <div>

  </div>;
};

export default MoreLikes;
