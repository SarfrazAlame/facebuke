import fetchPosts, { fetchPost } from "@/lib/fetch";
import React from "react";

const Comments = async ({ params }: { params: string | string[] }) => {
// const post = fetchPost()
  return <div>qew</div>;
};

export default Comments;
